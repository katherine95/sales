<%= form_with(model: sale, local: true) do |form| %>
  <% if sale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sale.errors.count, "error") %> prohibited this sale from being saved:</h2>

      <ul>
      <% sale.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :sale_email %>
  </div>

  <div class="field">
    <%= form.label :guid %>
    <%= form.text_field :guid, id: :sale_guid %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id, id: :sale_product_id %>
  </div>

  <div class="field">
    <%= form.label :stripe_id %>
    <%= form.text_field :stripe_id, id: :sale_stripe_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
